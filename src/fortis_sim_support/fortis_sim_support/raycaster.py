"""
Depth raycasting core (stub).

Will cast per-pixel rays from a pinhole camera model into the
synthetic_scene geometry and return metric depth images -- the pure
math under the renderer, kept free of ROS imports so it is
unit-testable in isolation.
"""
