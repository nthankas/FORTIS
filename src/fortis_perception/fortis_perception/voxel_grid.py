"""
Pure voxel-grid logic (stub).

Will hold the occupancy voxel grid data structure and update math used
by voxel_map_node: point insertion, decay, and export to PointCloud2
arrays. Named voxel_grid (not voxel_map) so the module does not shadow
the voxel_map node name. Kept free of ROS imports so it is
unit-testable in isolation.
"""
