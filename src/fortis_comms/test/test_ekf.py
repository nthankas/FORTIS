"""
Tests for the 6-state EKF.

The EKF module moved to ``legacy/deprecated_ekf/ekf.py``. Replaced by
ros2_control + standard ROS 2 packages. This file is intentionally
empty -- pytest will collect it as a no-op module rather than fail on
a missing import.
"""
